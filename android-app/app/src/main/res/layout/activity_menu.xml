<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="#000000"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MenuActivity">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- 🔙 Geri butonu -->
        <ImageButton
            android:id="@+id/btnBack"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_back_arrow"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Back"
            android:tint="#FFFFFF"
            android:layout_gravity="start"
            android:layout_marginBottom="16dp"/>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Control Menu"
            android:textColor="#FFFFFF"
            android:textSize="24sp"
            android:textStyle="bold"
            android:gravity="center"
            android:layout_marginBottom="24dp" />

        <GridLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:columnCount="2"
            android:alignmentMode="alignMargins"
            android:useDefaultMargins="true"
            android:layout_gravity="center_horizontal">

            <Button
                android:id="@+id/btnFreeMove"
                style="@style/MenuButtonStyle"
                android:layout_width="150dp"
                android:layout_height="150dp"
                android:background="@drawable/bg_btn_freemove"
                android:text="FREE MOVEMENT" />

            <Button
                android:id="@+id/btnGridDrawn"
                android:layout_width="150dp"
                android:textColor="#FFFFFF"
                android:layout_height="150dp"
                android:background="@drawable/bg_btn_hnddrwn"
                android:text="HAND DRAWN NAVIGATION" />

            <Button
                android:id="@+id/btnGpsTracking"
                android:layout_width="150dp"
                android:textColor="#FFFFFF"
                android:layout_height="150dp"
                android:background="@drawable/bg_btn_gpstrckng"
                android:text="GPS TRACKING" />

            <Button
                android:id="@+id/btnFollowMe"
                android:layout_width="150dp"
                android:textColor="#FFFFFF"
                android:layout_height="150dp"
                android:background="@drawable/bg_btn_flwme"
                android:text="FOLLOW ME" />

            <Button
                android:id="@+id/btnCalibration"
                android:textColor="#FFFFFF"
                android:layout_width="150dp"
                android:layout_height="150dp"
                android:background="@drawable/bg_btn_calibrationsetting"
                android:text="CALIBRATION SETTINGS" />

            <Button
                android:id="@+id/btnWifiAttack"
                android:layout_width="150dp"
                android:textColor="#FFFFFF"
                android:layout_height="150dp"
                android:background="@drawable/bg_btn_wifiattack"
                android:text="WIFI ATTACK" />
        </GridLayout>
    </LinearLayout>
</ScrollView>
